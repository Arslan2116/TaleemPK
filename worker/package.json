{
  "name": "taleempk-social-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "dev":    "wrangler dev social-worker.js",
    "test":   "wrangler dev social-worker.js --port 8788"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
