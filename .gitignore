# Editor / tooling
.claude/
.vscode/
.DS_Store
Thumbs.db

# Stray / scratch files (not part of the site)
colors.html
migrate.sql
# internal roadmap — must NOT be publicly deployed
UPCOMING-FEATURES.html

# Data agent (local dev tool — keep keys & output out of the repo/site)
agent/

# Cloudflare Wrangler local cache
worker/.wrangler/

# Build intermediate (list of generated SEO page slugs)
.seo-pages.json

# Kaggle source data (local build input only — never deploy)
*.csv
merit-formulas.json
match-map.json
new-unis.json
